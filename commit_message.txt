fix(map-generator): Corregir error de serialización int64 en JSON y SQLite

- Implementación de la clase NpEncoder heredada de json.JSONEncoder y función recursiva sanitize_for_json para convertir de forma estricta los tipos NumPy (int64, float64, ndarray) a tipos primitivos de Python.
- Conversión explícita a tipos nativos (int, float, str) en los cálculos de adyacencias topológicas de provincias terrestres (neighbors) y zonas marítimas adyacentes (adjacent_seas).
- Corrección de la estructura de inserción en SQLite (world_map.db) para las tablas 'provinces' y 'sea_zones', garantizando compatibilidad total con Android y consultas directas sin saturar la ventana de contexto de modelos de IA.
